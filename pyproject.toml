[project]
name = "webmon-bot"
version = "0.1.0"
description = "A client for Webmon, an AI research assistant."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.7.1",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
